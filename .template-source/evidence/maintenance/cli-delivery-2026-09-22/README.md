# 2026-09-22 GitHub 源码与 CLI 交付

本轮按用户“提交 git，完成 CLI 升级后推送 GitHub”的授权交付主模板、三个 Agent 和四个 CLI。维护强度为 L3，采用维护者自检及 Fresh Verification。模板身份为 template-source，产品 Context 对账与产品阶段批准不适用。

CLI 使用分发源码提交 `445172278b7a107ac42ec00ee1dd936bdd244933` 及各 Agent 完整提交重建，全部为 committed 来源：create-yss-spec 3.4.10、Design 0.8.5、Backend 0.4.9、Frontend 0.3.9。主仓后续提交只记录分发引用、模板源测试准备和本次证据，不改变这批 CLI 的实例分发内容。

四个 CLI 回归、包安装、初始化、接管、同步和诊断通过；Spec 回归 187/187，插件工具测试 142/142，契约效率专项 12/12。完整 `scripts/verify-template --concurrency 1` 通过：计划含 89 条命令，去重后实际执行 88 条，全部退出码为 0。具体命令、来源 SHA、包摘要和退出码见 verification.json；完整日志以 gzip 原样保留。

验证返工：插件原测试依赖开发工作区的忽略快照，且旧迁移只支持已登记的固定 CLI。现在测试从原始固定提交创建隔离检出，重建并核对历史快照，不修改 CLI 工作区，不更新迁移基线。旧 Spec 快照的 Skill 锁使用中文文件名排序，重建时显式复现该语言环境。插件 CLI 来源参数仍经过原有 pin、快照及干净工作区校验。

并行检查曾出现测试 HTTP 服务连接中断；相应场景及生成实例接力复验通过，最终全部检查采用串行执行，未放宽断言或修改产品探测规则。

原有 202 个未跟踪审查和测试证据文件按 SHA-256 核对保持不变，继续留在原目录。本轮不执行 npm publish；本记录不替代正式发布工作流的兼容矩阵或真实项目交付验收。
