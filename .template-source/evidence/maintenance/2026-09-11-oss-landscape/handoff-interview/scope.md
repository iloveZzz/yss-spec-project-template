# 跨仓交付可靠性试验访谈

身份：template-source；当前只访谈与查证，不实现试验或修改治理代码。

用户已确认（本任务原始回复“按推荐继续”）：Q1=A，首先验证现有机制可靠性，同时记录人工成本；Q2=A，选已完成真实需求，在隔离副本重放。

用户第二轮确认（对 Q3–Q5 回复“按推荐继续”）：首轮验证旧接口、错仓/错切片包、未绑定当前内容的旧批准；保留正确交付对照，排除业务实现错误、恶意伪造和权限绕过。拒收后已有受管文件、批准记录和交付状态保持原状，只允许独立失败报告/日志。修正输入及必要的有效批准后从正常入口重试，不手改摘要、删批准或强制跳过。

用户第三轮确认（对 Q6–Q7 回复“按推荐继续”）：固定权威基线，区分已知新版导致旧包拒收与上游变化尚未同步时的实际表现；首轮不新建在线最新性/批准撤销服务。允许保留独立、不可变且可核验的已导入战略快照，失败后端交付不得产生有效接收；已有文件、批准和接收状态不被覆盖或错误推进，正常重试可以复用快照。Q7 是对 Q4“只允许新增日志”的明确收窄，不追溯修改原始回复。

用户第四轮确认（对 Q8–Q10 回复“按推荐继续”）：选委外文件同步“目标表数据预览”为候选，先核对契约、固定前后端版本并补做基线验证，不把历史资产宣称为已完成基线；需要修改业务功能才能建立基线时暂停重新选样。所有预定场景必须符合预期，包括正确接收、幂等、三类故障对应拒收、状态保留、正常修复重试、干净环境一致重放；人工步骤和耗时仅记录。发现机制缺口先完成原样验证并输出复现/建议，不直接修复。

用户第五轮确认（对 Q11–Q12 回复“按推荐继续”）：交付方与接收方分开执行；接收方只使用自己的前端仓、交付包和登记的服务入口，不读取后端源码或原始批准目录；主控可对照两端证据但不能补缺。隔离目录和读取范围不宣称为安全沙箱。准备范围允许隔离配置、合成数据及实际执行生成的验证记录；业务代码修改、版本探针新增、项目接入或必要批准缺失先列前置缺口，暂停相关执行并单独决定，不伪造历史批准或日志。

用户最终确认：对 Q13 及整份试验约定回复“采纳推荐，确认”。采用本地隔离 PostgreSQL 控制库与目标表、真实 Java 服务及 Vue 页面、合成数据；首轮不覆盖 MySQL/Oracle。访谈已结束，下一步先整理前置缺口方案，不直接启动试验。原始回复和展示快照摘要见 confirmation.json。具体执行路径、基线提交、接入资产和批准记录仍是待落实前提，不把访谈确认当成这些资产已经存在或获批。

事实查证：检查已登记实现仓与已完成切片线索、Java/Vue 配对和交付证据、现有接收校验与运行入口。只读；不得安装、启动服务、改外仓、提交推送、造测试完成记录。

词汇对账 not-applicable：模板维护探索，不登记产品词汇。原有 Git 暂存修改保持原状。

补充候选发现范围：Codex 已保存项目列表显示委外产品 `/Users/zhudaoming/yss-valuation-outsourced`、模型管理 `/Users/zhudaoming/yss-datamiddle-datamodeling`、系统管理 `/Users/zhudaoming/Documents/yss-project/yss-datamiddle-sysmanager`。仅为查证候选读取这些项目入口和登记，不做生命周期接入，不修改外仓。身份文件缺失时如实记录，不能据目录猜项目身份。

候选已获访谈选用后继续只读查证其构建/启动入口、数据库与外部依赖、最小预览场景、版本探针是否存在、独立前端 HEAD/脏状态。仅读取公开构建定义和文档，不读取凭据或数据，不运行安装、构建、服务、容器和产品测试。
