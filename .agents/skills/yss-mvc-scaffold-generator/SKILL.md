---
name: yss-mvc-scaffold-generator
description: 已退役的数据分析 MVC 初始化旧入口；仅用于识别历史合同并指引重新批准，不执行生成或自动迁移。
---

# 已退役入口

本 ID 为 deprecated，只读保留历史合同解释。通用 MVC 使用 yss-layered-mvc-scaffold-generator；独立数据分析项目使用 yss-mvc-data-analysis-project-initializer。旧 Oracle/Mock 合同不得静默别名或重放；经生命周期确认 Profile 与 H2 新合同后才执行新入口。历史说明位于 references/history/skill.md，不是现行执行规范。
