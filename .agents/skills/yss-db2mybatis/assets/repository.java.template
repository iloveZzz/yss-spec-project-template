package ${base_package}.repository;

import com.yss.cloud.mybatis.support.BasePlusRepository;
import ${base_package}.repository.entity.${domain_name}PO;

public interface ${domain_name}Repository extends BasePlusRepository<${domain_name}PO> {
}
