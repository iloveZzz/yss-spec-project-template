# Comet Pipeline 配置
# 每个 Feature 一个 pipeline

pipelines:
  # 示例:
  # my-feature:
  #   spec: docs/api/specs/my-feature.yaml
  #   current_stage: open
  #   sprint: "Sprint 1"
  #   stages:
  #     open:    { status: pending }
  #     design:  { status: pending }
  #     build:   { status: pending }
  #     verify:  { status: pending }
  #     archive: { status: pending }

meta:
  version: "1.0"
  template: "hermes-project-template"
